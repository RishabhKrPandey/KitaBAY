const mongoose = require('mongoose')

const cartModel = mongoose.Schema({
    // when clicked on add to cart book details will be stored in backend
    // all prices shown with delete and add options
    
})